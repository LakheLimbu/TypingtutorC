#ifndef TEXTSAMPLES_H
#define TEXTSAMPLES_H

#include <string>

const std::string samples[] = {
    "int main() { return 0; }",
    "std::cout << \"Hello, World!\" << std::endl;",
    "for(int i = 0; i < 10; ++i) { std::cout << i << std::endl; }",
    "if(x == 42) { std::cout << \"The answer to life.\" << std::endl; }",
    "std::vector<int> numbers = {1, 2, 3, 4, 5};"
};

#endif // TEXTSAMPLES_H
